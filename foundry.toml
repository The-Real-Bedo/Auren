[profile.default]
fs_permissions = [{ access = "read-write", path = "./" }]
src = "src"
out = "out"
libs = ["lib"]
via_ir = true
optimizer = true
optimizer_runs = 200

[fuzz]
runs = 10000

[invariant]
runs = 1000
depth = 100
